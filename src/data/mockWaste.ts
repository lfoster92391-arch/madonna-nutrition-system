/** Unused by cafeteria waste UI — live logs come from InventoryMovement. Kept for older mock imports. */
export const mockWaste: Array<{
  id: number
  itemName: string
  quantityWasted: number
  reason: string
  date: string
}> = []
